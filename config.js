const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      HOST: "localhost",
      USER: "admin",
      PASSWORD: "=@!#254tecmint",
      DB: "cafe_management",
    },
    listPerPage: 10,
  };
module.exports = config;